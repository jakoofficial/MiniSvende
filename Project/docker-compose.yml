services:
  mysql:
    image: mysql:latest
    container_name: mintyMysql
    ports:
      - "3306:3306"
    environment:
      - MYSQL_ROOT_PASSWORD=admin
      - MYSQL_DATABASE=minty
    volumes:
      - ./data:/var/lib/mysql